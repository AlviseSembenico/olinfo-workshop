[
  {
    "objectID": "lectures/lecture-1.html",
    "href": "lectures/lecture-1.html",
    "title": "Lecture 1 — Trees as Arrays",
    "section": "",
    "text": "Learn to turn a tree into an array so that familiar range-query tools can answer subtree and ancestry questions efficiently.\n\n\n\n\n\n\nNoteSuggested class\n\n\n\nCore sequence: Subtree Queries → Company Queries II → Counting Paths\nPrerequisites: DFS, arrays, and a Fenwick tree or segment tree.\nMain pattern: tree → intervals / ancestors → range structure or accumulation.",
    "crumbs": [
      "Lectures",
      "Lecture 1: Trees as Arrays"
    ]
  },
  {
    "objectID": "lectures/lecture-1.html#goal",
    "href": "lectures/lecture-1.html#goal",
    "title": "Lecture 1 — Trees as Arrays",
    "section": "",
    "text": "Learn to turn a tree into an array so that familiar range-query tools can answer subtree and ancestry questions efficiently.\n\n\n\n\n\n\nNoteSuggested class\n\n\n\nCore sequence: Subtree Queries → Company Queries II → Counting Paths\nPrerequisites: DFS, arrays, and a Fenwick tree or segment tree.\nMain pattern: tree → intervals / ancestors → range structure or accumulation.",
    "crumbs": [
      "Lectures",
      "Lecture 1: Trees as Arrays"
    ]
  },
  {
    "objectID": "lectures/lecture-1.html#problem-sequence",
    "href": "lectures/lecture-1.html#problem-sequence",
    "title": "Lecture 1 — Trees as Arrays",
    "section": "Problem sequence",
    "text": "Problem sequence\n\n1. Subtree sums and intervals\n\nSubtree Queries — CSES: point-update a node and query its subtree sum. This is the first implementation of Euler-tour flattening plus a Fenwick tree or segment tree.\nSubtree Sum: use the same setup as a short warm-up. First ask students how the answer would be computed if every subtree were already an interval.\n\n\n\n\n\n\n\nTipSolution — Subtree Queries (C++)\n\n\n\n\n\n#include &lt;iostream&gt;\n#include &lt;vector&gt;\n#include &lt;unordered_map&gt;\n#include &lt;unordered_set&gt;\n#include &lt;algorithm&gt;\n#include &lt;sstream&gt;\n#include &lt;cmath&gt;\n#include &lt;map&gt;\n#include &lt;set&gt;\n#include &lt;numeric&gt;\n#include &lt;climits&gt;\n\nusing namespace std;\n\n#define DEBUG 0\n\n#if DEBUG\n#define HAS_EXTRA 1\n#include \"../codeforces/debug.hpp\"\n#endif\n\ntypedef long long int ll;\ntypedef vector&lt;int&gt; vint;\n\nbool intersect(int l1, int r1, int l2, int r2)\n{\n    return max(l1, l2) &lt;= min(r1, r2);\n}\n\nclass SegTree\n{\n    void _update(int p, ll val, int cp, int l, int r)\n    {\n        if (p &lt; l || p &gt; r)\n            return;\n\n        if (l == r)\n        {\n            tree[cp] = val;\n            return;\n        }\n\n        int tm = (l + r) / 2;\n\n        _update(p, val, cp * 2, l, tm);\n        _update(p, val, cp * 2 + 1, tm + 1, r);\n\n        tree[cp] = tree[cp * 2] + tree[cp * 2 + 1];\n    }\n\n    void build(vector&lt;ll&gt; &a, int v, int tl, int tr)\n    {\n        if (tl == tr)\n        {\n            tree[v] = a[tl];\n        }\n        else\n        {\n            int tm = (tl + tr) / 2;\n\n            build(a, v * 2, tl, tm);\n            build(a, v * 2 + 1, tm + 1, tr);\n\n            tree[v] = tree[v * 2] + tree[v * 2 + 1];\n        }\n    }\n\n    ll _sum(int v, int tl, int tr, int l, int r)\n    {\n        if (l &gt; r)\n            return 0LL;\n\n        if (l == tl && r == tr)\n            return tree[v];\n\n        int tm = (tl + tr) / 2;\n\n        return _sum(v * 2, tl, tm, l, min(r, tm)) +\n               _sum(v * 2 + 1, tm + 1, tr, max(l, tm + 1), r);\n    }\n\npublic:\n    vector&lt;ll&gt; tree;\n    int size;\n\n    SegTree(vector&lt;ll&gt; s)\n    {\n        size = s.size();\n        tree.resize(4 * size);\n        build(s, 1, 0, size - 1);\n    }\n\n    ll sum(int l, int r)\n    {\n        return _sum(1, 0, size - 1, l, r);\n    }\n\n    void update(int pos, ll v)\n    {\n        _update(pos, v, 1, 0, size - 1);\n    }\n};\n\nvoid DFSFlattening(\n    vector&lt;vector&lt;ll&gt;&gt; &graph,\n    vector&lt;ll&gt; &nodes,\n    int node,\n    int prev,\n    vector&lt;ll&gt; &res,\n    vint &start,\n    vint &end)\n{\n    start[node] = res.size();\n    res.push_back(nodes[node]);\n\n    for (int child : graph[node])\n    {\n        if (child == prev)\n            continue;\n\n        DFSFlattening(graph, nodes, child, node, res, start, end);\n    }\n\n    end[node] = res.size() - 1;\n}\n\nint main()\n{\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n\n    ll n, q;\n    cin &gt;&gt; n &gt;&gt; q;\n\n    vector&lt;ll&gt; nodes(n + 1);\n\n    for (ll i = 0; i &lt; n; i++)\n        cin &gt;&gt; nodes[i + 1];\n\n    vector&lt;vector&lt;ll&gt;&gt; edges(n + 1);\n\n    for (ll i = 0; i &lt; n - 1; i++)\n    {\n        ll f, t;\n        cin &gt;&gt; f &gt;&gt; t;\n\n        edges[f].push_back(t);\n        edges[t].push_back(f);\n    }\n\n    vector&lt;ll&gt; flat;\n    vint start(n + 1);\n    vint end(n + 1);\n\n    DFSFlattening(edges, nodes, 1, -1, flat, start, end);\n\n    SegTree sg(flat);\n\n    for (ll i = 0; i &lt; q; i++)\n    {\n        ll type;\n        cin &gt;&gt; type;\n\n        if (type == 1)\n        {\n            ll s, x;\n            cin &gt;&gt; s &gt;&gt; x;\n\n            sg.update(start[s], x);\n        }\n        else\n        {\n            ll s;\n            cin &gt;&gt; s;\n\n            ll res = sg.sum(start[s], end[s]);\n            cout &lt;&lt; res &lt;&lt; '\\n';\n        }\n    }\n}\n\n\n\n\n\n2. Ancestors and lowest common ancestors\n\nCompany Queries I — CSES: find the kth ancestor. Binary lifting without any LCA logic.\n\n\n\n\n\n\n\nTipSolution hint — Company Queries I\n\n\n\n\n\nReview the CP-Algorithms guide to binary lifting for LCA. For this problem, use its up[u][j] table only for upward jumps: up[u][j] is the 2^jth ancestor of u.\n\n\n\n\nCompany Queries II — CSES: answer LCA queries. This is the natural continuation after kth ancestor.\nDistance Queries — CSES: connect LCA to the formula dist(a, b) = depth[a] + depth[b] - 2 × depth[lca(a, b)].\n\n\n\n3. Combining the ideas\n\nPath Queries — CSES: update a node value and query a root-to-node path. Discuss what needs to be stored differently from a subtree sum.\nCounting Paths — CSES: the capstone. For every requested path (a, b), add a small number of changes and combine them with one final DFS.\n\nFor Counting Paths, if w = lca(a, b), apply the tree-difference updates below, then sum child values upward in a post-order DFS:\ndelta[a]++;\ndelta[b]++;\ndelta[w]--;\nif (parent[w] != w) delta[parent[w]]--;\n\nvoid accumulate(int u, int p) {\n  for (int v : graph[u]) {\n    if (v == p) continue;\n    accumulate(v, u);\n    delta[u] += delta[v];\n  }\n}\nThe important question is not “what template should I use?” but “which contributions should cancel once they meet above the LCA?”\n\n\n\n\n\n\nTipThe central idea: a subtree is an interval\n\n\n\n\n\nRun a DFS and assign each node the time at which it is first visited. If we visit a whole subtree before leaving it, the nodes in the subtree of u occupy one contiguous interval: [tin[u], tout[u]].\nint timer = 0;\nconst int LOG = 20;\nvector&lt;vector&lt;int&gt;&gt; graph;\nvector&lt;int&gt; tin, tout, parent, depth;\nvector&lt;array&lt;int, LOG&gt;&gt; up;\n\nvoid dfs(int u, int p) {\n  parent[u] = p;\n  up[u][0] = p;\n  tin[u] = timer++;\n  for (int v : graph[u]) {\n    if (v == p) continue;\n    depth[v] = depth[u] + 1;\n    dfs(v, u);\n  }\n  tout[u] = timer - 1;\n}\n\n\n\nA rooted tree and its DFS order. Each subtree occupies one consecutive interval in the array.\n\n\nWith this representation, a node update becomes a point update at tin[u], and a subtree sum becomes a range sum from tin[u] to tout[u].\n// Store value[u] at position tin[u].\nfenwick.add(tin[u], new_value - value[u]);\nvalue[u] = new_value;\n\nlong long subtree_sum(int u) {\n  return fenwick.sum(tout[u]) - fenwick.sum(tin[u] - 1);\n}",
    "crumbs": [
      "Lectures",
      "Lecture 1: Trees as Arrays"
    ]
  },
  {
    "objectID": "lectures/lecture-1.html#ancestors-in-logarithmic-time",
    "href": "lectures/lecture-1.html#ancestors-in-logarithmic-time",
    "title": "Lecture 1 — Trees as Arrays",
    "section": "Ancestors in logarithmic time",
    "text": "Ancestors in logarithmic time\nBinary lifting stores the node reached after jumping upward by 2^j edges. The table lets us find a kth ancestor by decomposing k into powers of two.\nvoid build_lifting(int n) {\n  for (int j = 1; j &lt; LOG; ++j)\n    for (int u = 0; u &lt; n; ++u)\n      up[u][j] = up[up[u][j - 1]][j - 1];\n}\n\nint kth_ancestor(int u, int k) {\n  for (int j = 0; j &lt; LOG; ++j)\n    if (k & (1 &lt;&lt; j)) u = up[u][j];\n  return u;\n}\nFor LCA, first lift the deeper node until both nodes have the same depth; then lift both nodes from the largest possible jump downward until their parents agree.\nint lca(int a, int b) {\n  if (depth[a] &lt; depth[b]) swap(a, b);\n  a = kth_ancestor(a, depth[a] - depth[b]);\n  if (a == b) return a;\n\n  for (int j = LOG - 1; j &gt;= 0; --j) {\n    if (up[a][j] != up[b][j]) {\n      a = up[a][j];\n      b = up[b][j];\n    }\n  }\n  return up[a][0];\n}",
    "crumbs": [
      "Lectures",
      "Lecture 1: Trees as Arrays"
    ]
  },
  {
    "objectID": "lectures/lecture-1.html#challenge-subtree-and-ancestor-queries",
    "href": "lectures/lecture-1.html#challenge-subtree-and-ancestor-queries",
    "title": "Lecture 1 — Trees as Arrays",
    "section": "Challenge: subtree and ancestor queries",
    "text": "Challenge: subtree and ancestor queries\nBuild a structure supporting subtree updates or queries together with ancestry tests. A node u is an ancestor of v exactly when:\ntin[u] &lt;= tin[v] && tout[v] &lt;= tout[u]\nAsk students to decide which operations become interval operations and which require the ancestor relation directly.",
    "crumbs": [
      "Lectures",
      "Lecture 1: Trees as Arrays"
    ]
  },
  {
    "objectID": "lectures/lecture-1.html#before-coding",
    "href": "lectures/lecture-1.html#before-coding",
    "title": "Lecture 1 — Trees as Arrays",
    "section": "Before coding",
    "text": "Before coding\nFor each problem, answer these questions first:\n\nWhat does the DFS order make contiguous?\nIs the query about a subtree, a root path, or a path between two nodes?\nWhich information must be combined at the LCA?\nIs a Fenwick tree sufficient, or is a segment tree actually needed?",
    "crumbs": [
      "Lectures",
      "Lecture 1: Trees as Arrays"
    ]
  },
  {
    "objectID": "lectures/lecture-1.html#takeaway",
    "href": "lectures/lecture-1.html#takeaway",
    "title": "Lecture 1 — Trees as Arrays",
    "section": "Takeaway",
    "text": "Takeaway\nWhen a tree problem looks unfamiliar, first ask whether a traversal can expose an interval, whether binary lifting exposes the relevant ancestor, or whether path contributions can cancel through an LCA.",
    "crumbs": [
      "Lectures",
      "Lecture 1: Trees as Arrays"
    ]
  },
  {
    "objectID": "resources.html",
    "href": "resources.html",
    "title": "Resources",
    "section": "",
    "text": "This page is a home for material shared across lectures."
  },
  {
    "objectID": "resources.html#reference-material",
    "href": "resources.html#reference-material",
    "title": "Resources",
    "section": "Reference material",
    "text": "Reference material\n\nUSACO Guide — A structured competitive programming guide with topic explanations and curated practice problems.\nCP-Algorithms — Explanations and implementations of algorithms and data structures used in competitive programming.\nAlvise Sembenico’s CP repository — Alvise Sembenico’s GitHub repository dedicated to competitive programming.\nOlinfo Training — An online judge for practicing with problems from Italian informatics olympiads and other programming contests.\nCompetitive Programmer’s Handbook — Antti Laaksonen’s handbook covering competitive programming algorithms and techniques, linked to PDF page 147."
  },
  {
    "objectID": "resources.html#practice-platforms",
    "href": "resources.html#practice-platforms",
    "title": "Resources",
    "section": "Practice platforms",
    "text": "Practice platforms\n\nCodeforces — Regular programming contests and a large problem archive with difficulty ratings and editorials.\nAtCoder — Programming contests for a range of skill levels, with past problems available for practice.\nCSES — A collection of practice problems organized by topic to build core algorithms and data structures skills."
  },
  {
    "objectID": "resources.html#development",
    "href": "resources.html#development",
    "title": "Resources",
    "section": "Development",
    "text": "Development\n\nUSACO Guide IDE — A browser-based IDE for writing and running competitive programming solutions without a local setup.\nVisual Studio Code with CPH Judge — A local editor with an extension for running solutions against sample and custom test cases.\n\nFor faster C++ compilation in VS Code, follow the compilation instructions in Alvise Sembenico’s CP repository, which use Clang and precompiled headers. The example commands target Windows; adapt the paths to your local setup."
  },
  {
    "objectID": "resources.html#c-shorthand-and-type-aliases",
    "href": "resources.html#c-shorthand-and-type-aliases",
    "title": "Resources",
    "section": "C++ shorthand and type aliases",
    "text": "C++ shorthand and type aliases\nThe lecture solutions and the CP repository’s template use short names for common types and operations. The examples below assume using namespace std;, as in the repository, so vector means std::vector and pair means std::pair.\n\nReading typedef and using\ntypedef existing_type short_name; gives an existing type another name. The modern spelling is using short_name = existing_type;. Both are aliases: they have the same behavior and memory requirements as the original type.\ntypedef long long int ll;\n// Equivalently: using ll = long long int;\n\ntypedef vector&lt;ll&gt; vlong;\n// Equivalently: using vlong = vector&lt;ll&gt;;\n\nll answer = 0;\nvlong distance(5, -1);  // Five long long values, initially -1.\n\n\n\n\n\n\n\n\nAlias\nFull type\nTypical use\n\n\n\n\nll\nlong long int\nLarge sums, products, distances, and counts.\n\n\nlint\nlong long int\nAn alternative name found in older solutions.\n\n\nvint\nvector&lt;int&gt;\nInteger arrays, node indices, and adjacency lists.\n\n\nvlong\nvector&lt;ll&gt;\nArrays whose values can exceed the range of int.\n\n\npii\npair&lt;int, int&gt;\nTwo indices, coordinates, or endpoints.\n\n\npil\npair&lt;int, ll&gt;\nAn index followed by a large value or weight.\n\n\npll\npair&lt;ll, ll&gt;\nTwo large values.\n\n\nGrid2D\nvector&lt;vector&lt;int&gt;&gt;\nA two-dimensional grid; some solutions use ll elements instead.\n\n\nGrid3D\nvector&lt;Grid2D&gt;\nA three-dimensional grid using that file’s Grid2D definition.\n\n\nT in Flight_Routes.cpp\npair&lt;ll, int&gt;\nA local alias for a distance–node pair in a priority queue.\n\n\n\nThe order in a pair matters: pil stores the int in .first and the ll in .second. Pairs compare lexicographically: first by .first, then by .second to break ties.\nOn typical contest platforms, int is 32 bits and ll is 64 bits. Storing a result in ll does not prevent an earlier int multiplication from overflowing: write ll product = 1LL * a * b; when a and b are int and their product needs 64 bits. The LL suffix makes the multiplication use long long from the start.\n\n\nMacros and helpers\nThese names are defined by the solution template; they are not built into C++. A macro performs text substitution before compilation.\n\n\n\n\n\n\n\nShorthand\nMeaning or expansion\n\n\n\n\nv.pb(x)\nv.push_back(x): append an element.\n\n\nall(v)\nv.begin(), v.end(): pass the whole iterator range, as in sort(all(v)).\n\n\nloop(a, b)\nfor (int i = a; i &lt; b; i++): visit indices in [a, b).\n\n\nloop0(n)\nfor (int i = 0; i &lt; n; i++): visit indices from 0 to n - 1.\n\n\nrange(a, b)\nAn alternative spelling of loop(a, b) in some solutions.\n\n\ncontains(v, x) / icontains(v, x)\nSearch for x with find and return whether it was found. This is a linear scan, including when the macro is given a set.\n\n\npp(a, b)\nmake_pair(a, b): construct a pair, inferring its element types.\n\n\ngraph\nvector&lt;vector&lt;int&gt;&gt;: an adjacency-list type in Course_Schedule.cpp.\n\n\nmake_2d(rows, cols, value)\nConstruct a Grid2D initialized to value, which defaults to zero.\n\n\nmake_3d(a, b, c, value)\nConstruct a Grid3D with the given dimensions and initial value.\n\n\n\nvint values = {3, 1, 2};\nvalues.pb(4);\nsort(all(values));\nloop0(values.size()) {\n    cout &lt;&lt; values[i] &lt;&lt; ' ';  // 1 2 3 4\n}\nBoth loop macros introduce a variable named i. Use ordinary loops with different variable names when nesting them. Macros can also repeat their arguments, so pass simple variables rather than expressions with side effects such as i++. For a set or map, use its member .find(x) or .contains(x) instead of the linear-search macro; member .contains requires C++20.\nYou will also see auto for an inferred type and structured bindings such as auto [node, weight] = edge; to unpack a pair. auto [a, b] copies its elements, auto& [a, b] refers to the originals, and const auto& [a, b] reads them without copying. These bindings require C++17 or later.\nThe template’s ios::sync_with_stdio(false); cin.tie(nullptr); speeds up stream input and output. After disabling synchronization, use C++ streams consistently instead of mixing them with scanf or printf; flush explicitly when an interactive problem needs output immediately. &lt;bits/stdc++.h&gt; is a convenience header provided by libstdc++, not a standard C++ header, so its availability depends on your compiler setup."
  },
  {
    "objectID": "resources.html#debugging",
    "href": "resources.html#debugging",
    "title": "Resources",
    "section": "Debugging",
    "text": "Debugging\n\nUnderstand the template’s switches\nThe repository uses separate switches for logging, extra printing helpers, and local input. They are independent.\n\n\n\n\n\n\n\nSwitch\nWhat it controls\n\n\n\n\nDEBUG\nIn the usual #if DEBUG block, setting it to 1 includes codeforces/debug.hpp; setting it to 0 skips that block.\n\n\nHAS_EXTRA\nA marker defined alongside that include in several templates. It does not enable or disable deb(...).\n\n\nONLINE_JUDGE\nWhen defined, the template replaces deb(...) with an empty macro. When absent, deb(...) calls logger, even if DEBUG is 0.\n\n\nCINPUT\nWhen defined, templates with the corresponding block read from input.txt using freopen.\n\n\n\n#if DEBUG checks the macro’s value, while #ifdef DEBUG only checks whether it exists. A few files use the latter: #define DEBUG 0 still enables an #ifdef DEBUG block. Check which form your solution uses.\n\n\nPrint values while investigating a solution\nWith the template’s logger available and ONLINE_JUDGE undefined:\nint n = 5;\nll answer = 42;\ndeb(n, answer);  // Prints: n, answer = 5, 42\ndeb uses #__VA_ARGS__ to turn the argument expressions into a label, then sends their values to logger. The logger works with types that support operator&lt;&lt;, such as numbers and strings; it does not automatically print vectors or pairs. It writes to cout and does not add a newline.\nFor containers, debug.hpp provides printVector(v) for vectors of int or long long, printVector(a, n) for arrays of those types, and printMap(m) for map&lt;int, int&gt;. The header uses C++20 concepts. Set the existing DEBUG definition to 1, make sure the header can be found, and guard calls with the same condition as the include:\n#if DEBUG\nprintVector(values);\nprintMap(frequency);\n#endif\nThe quoted include path must resolve from the source file or a compiler include directory. If compiling from the CP repository root, adding -I . lets the compiler find codeforces/debug.hpp even when the solution is in a subdirectory.\nThe current container helpers also write to cout, so their output can interfere with judged answers. For temporary diagnostics, use cerr, or adapt the logger and helpers to write to cerr, while keeping the required answer on cout:\ncerr &lt;&lt; \"node = \" &lt;&lt; node &lt;&lt; \", distance = \" &lt;&lt; distance[node] &lt;&lt; '\\n';\nKeep debug expressions free of side effects: a call such as deb(i++) changes behavior when the macro is disabled. Remove or guard diagnostics before submitting, including those sent to cerr, to avoid unnecessary output and runtime overhead.\n\n\nLocal input and VS Code breakpoints\nFor a local build with GCC available, run these commands from the CP repository root, replacing solution.cpp with your solution’s path:\ng++ -std=c++23 -O0 -g -Wall -Wextra -I . solution.cpp -o solution\n./solution &lt; input.txt\n-O0 makes stepping through the code easier and -g includes debugger symbols. The fast-compilation commands in the README use -g0, which disables those symbols; use -g when you need breakpoints.\nIf you use the template’s CINPUT block instead of shell redirection, compile with -DCINPUT=1 and put input.txt in the program’s working directory. The repository’s VS Code launch configuration uses the source file’s directory as its working directory.\nFor breakpoints, install the VS Code C/C++ extension and configure a debugger available on your platform. The repository’s build task and launch configuration show a Clang/LLDB setup. Set a breakpoint beside a line number, press F5, and inspect variables and the call stack; F10 steps over a statement and F11 steps into a function. Use CPH Judge to run sample and custom cases, and Run and Debug when you need to step through one case.\nThe current build task passes -DDEBUG1=1. DEBUG1 and DEBUG are different names, so this does not enable #if DEBUG. Also, a source file’s unconditional #define DEBUG 0 overrides a command-line -DDEBUG=1. Either edit that source definition for local debugging, or replace it with a default that allows command-line overrides:\n#ifndef DEBUG\n#define DEBUG 0\n#endif\nWith this default and #if DEBUG guards, adding -DDEBUG=1 enables the extra helpers during a local build.\nBefore submitting, disable the extra helpers and local file input, and ensure that deb(...) is disabled or removed. Defining ONLINE_JUDGE with -DONLINE_JUDGE lets you check that branch locally, but not every platform defines it for submissions. In this template, setting DEBUG to 0 alone does not disable deb(...); if needed, guard those calls with #if DEBUG as well."
  },
  {
    "objectID": "index.html",
    "href": "index.html",
    "title": "Olinfo Training Galilei",
    "section": "",
    "text": "Explore the lectures"
  },
  {
    "objectID": "index.html#what-you-will-learn",
    "href": "index.html#what-you-will-learn",
    "title": "Olinfo Training Galilei",
    "section": "What you will learn",
    "text": "What you will learn\n\n\n\nAlgorithmic thinking\nTurn a difficult problem into a sequence of ideas, invariants, and efficient algorithms.\n\n\n\n\nMathematical tools\nUse combinatorics, number theory, and proof techniques to understand why an algorithm works.\n\n\n\n\nContest practice\nDevelop the habits needed to read problems carefully, choose constraints wisely, and implement reliably."
  },
  {
    "objectID": "index.html#how-the-course-is-organised",
    "href": "index.html#how-the-course-is-organised",
    "title": "Olinfo Training Galilei",
    "section": "How the course is organised",
    "text": "How the course is organised\nEach section corresponds to an in-person lecture. A lecture page can contain preparation, notes, worked examples, exercises, and optional follow-up material. Start with the lecture overview or browse the sections in the sidebar."
  },
  {
    "objectID": "lectures/index.html",
    "href": "lectures/index.html",
    "title": "Lectures",
    "section": "",
    "text": "Lecture 1: Trees as Arrays\nLecture 2\nLecture 3\nLecture 4\nLecture 5",
    "crumbs": [
      "Lectures",
      "Overview"
    ]
  }
]